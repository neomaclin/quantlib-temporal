package org.quantlib.time

import org.scalatest._

/**
  * Created by neo on 12/03/2017.
  */
class DayCountBasisSpec extends FlatSpec with Matchers {
  "ActualActual" should "pass all testing for default policy." in {assert(true)}
  "Simple" should "pass all testing for default policy." in {assert(true)}
  "One" should "pass all testing for default policy." in {assert(true)}
  "Business252" should "pass all testing for default policy." in {assert(true)}
  "Thirty360_BondBasis" should "pass all testing for default policy." in {assert(true)}
  "Thirty360_EurobondBasis" should "pass all testing for default policy." in {assert(true)}
  "Intraday" should "pass all testing for default policy." in {assert(true)}
}