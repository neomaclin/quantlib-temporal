package org.quantlib.time

import org.scalatest._

/**
  * Created by neo on 12/03/2017.
  */
class DateTimeStandardSpec extends FlatSpec with Matchers {
  "date consistency" should "pass all test" in {assert(true)}
  "ecbDates" should "pass all test" in {assert(true)}
  "immDates" should "pass all test" in {assert(true)}
  "asxDates" should "pass all test" in {assert(true)}
  "isoDates" should "pass all test" in {assert(true)}
  "intraday"  should "pass all test" in {assert(true)}
}
